jsp文件在引入js文件的时候使用 <script type="text/javascript"  src="xxx.js"  charset="GBK"></script>,是可以设置打开文件的编码方式的，不仅如此，连里面动态创建的内容，都是使用charset设置的编码格式，即使你的文件保存格式是其他的格式也会被重置，当去掉这个charset的时候，使用的则是你的文件的编码格式。

另外，在测试相关代码的时候，可以选择每次打开google或者火狐的隐身模式，因为这样不会有浏览记录，不必每次都手工清楚缓存。
